import { HighLightsDirective } from './high-lights.directive';

describe('HighLightsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightsDirective();
    expect(directive).toBeTruthy();
  });
});
